# NEJEN screen-share portal settings.

screencopy {
    # Friendlier window/output picker for screen sharing
    custom_picker_binary = hyprland-preview-share-picker

    # Skip the re-confirmation dialog on repeated shares
    allow_token_by_default = true
}
