# NEJEN nightlight baseline.
#
# hyprsunset idles at identity (no tint); `nejen night toggle` switches
# the temperature at runtime. Schedule-based tinting can be added in
# ~/.config/hypr/overrides.conf if wanted.

profile {
    time = 00:00
    identity = true
}
